"""TagConnect TC2030 connector (with legs) for SWD interface"""

load("@stdlib/interfaces.zen", "Swd")

VSENSE = io(Power(voltage="1.2V to 5.5V"))
GND = io(Ground)
SWD = io(Swd)
NRST = io(Net)
SWO = io(Net)

moved("TAG_CONNECT", "J")

Component(
    name="J",
    symbol=Symbol(library="TagConnect.kicad_sym", name="Conn_ARM_SWD_TagConnect_TC2030"),
    pins={
        "VCC": VSENSE,
        "GND": GND,
        "SWCLK": SWD.SWCLK,
        "SWDIO": SWD.SWDIO,
        "~{RESET}": NRST,
        "SWO": SWO,
    },
    skip_bom=True,
    skip_pos=True,
)
